return {
    version = 1,
    identity = "LoveTower",  -- The name of the save directory (string)
    appendidentity = false, -- Search files in source directory before save directory (boolean)
    window = {
        title = "LoveTower",
        width = 1200,
        height = 800,
        vsync = 1,
    },
    console = false,
    modules = {
        joystick = false,
        physics = false,
    },
}
